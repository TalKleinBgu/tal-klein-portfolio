{
  "name": "tal-klein-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./style.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
